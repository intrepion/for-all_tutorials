# {{ title }}

Generated tutorial for the `{{ role_label }}` output of `{{ project_slug }}`.

Do not edit this file by hand. Regenerate it from `partials/`.

## Output Repository

- Name: `{{ repo_name }}`
- Description: {{ repo_description }}
- Generated Path: `{{ generated_path }}`

## Selected Stack

{% for item in selected_stack -%}
- {{ item.key }}: `{{ item.value }}`
{% endfor %}

{% for section in sections %}
{% if section.heading.len() > 0 -%}
## {{ section.heading }}

{% endif -%}
{{ section.body }}

{% endfor -%}
